name: Append Direct Sales Memory Entry

on:
  workflow_dispatch:
    inputs:
      message:
        description: 'Memory note to append'
        required: true
      filepath:
        description: 'Path to the markdown file'
        required: false
        default: 'direct_sales_memory_log.md'

jobs:
  append_memory:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Append message to markdown
        run: |
          python scripts/append_memory.py "${{ github.event.inputs.message }}" "${{ github.event.inputs.filepath }}"

      - name: Commit and push changes
        env:
          TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@users.noreply.github.com"
          echo "🔍 Checking if token is present..."
          echo "Using token: $TOKEN"

          git remote remove origin
          git remote add origin https://x-access-token:$TOKEN@github.com/${{ github.repository }}.git

          git add "${{ github.event.inputs.filepath }}"
          git commit -m "🧠 Direct Sales memory update" || echo "No changes to commit"
          git push origin HEAD
